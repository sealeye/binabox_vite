<section class="hero">
    <h1 class="hero__heading">{{ index.hero.heading.part_1 }} <span class="hightlight">{{ index.hero.heading.part_2 }}</span> {{ index.hero.heading.part_3 }}</h1>
    <p class="hero__text">{{ index.hero.text }}</p>
    <dl class="hero__defenition">
        <dt>2240+</dt>
        <dd>Total It eam</dd>
        <dt>1000+</dt>
        <dd>Profiles whitelisted</dd>
    </dl>
    <button class="banner__button button button--primary">Connect Wallet</button>
    <button class="banner__button button">{{ index.hero.whitelist_button }}</button>
</section>