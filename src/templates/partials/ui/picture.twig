<picture>
    <source srcset="{{ img.src }}, logo-1.5x.png 2x" />
    <source srcset="{{ img.src }}.webp" type="image/webp" />
    <img src="{{ img.src }}" alt="{{ img.alt }}" height="{{ img.width }}" width="{{ img.height }}" />
</picture>